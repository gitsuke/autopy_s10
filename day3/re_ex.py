# -*- coding:utf-8 -*-
# Author: suke
# At 2015/10/25 18:53

import re

# re.match  从字符串的起点开始做匹配
# re.search 从字符串做任意匹配。在字符串中查找，是否能匹配正则表达式
# re.findall 找到 RE 匹配的所有子串，并把它们作为一个列表返回
# re.split 通过正则表达式将字符串分离
# re.sub 找到 RE 匹配的所有子串，并将其用一个不同的字符串替换

